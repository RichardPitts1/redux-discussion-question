export const initialState = {
    stockData: []
}

export const reducer  = (state, action) => {
    switch(action.type){
        case 'YOUR_ACTION_HERE': // ?
            return {}  // ?
        break;
        
    }
    return state
}